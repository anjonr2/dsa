In this problem basically we are trying to find
Can we partition the array into k or fewer pieces if each piece is <= Mid?
Low values (e.g, 10): "No, it will make too many partitions" (Impossible)
High values (e.g 100) : Yes It can be done in fewer partitions (Possbile)